# Reused class and JavaScript contracts

| Source vocabulary | Reused in Aurelia |
|---|---|
| Open Design | `.container`, `.display`, `.label`, `.pill`, `.ring`, `.wire-item`, `.wire-dot`, `.wire-handle`, `.wire-role`, `.side-rail`, `.site-chrome`, `[data-chrome-headroom]`, `[data-reveal]` |
| Glass Pricing | `.ds-nav`, `.ds-section`, `.ds-section-title`, `.ds-card`, `.ds-spec-row`, `.ds-spec-name`, `.ds-spec-preview`, `.ds-spec-value`, `.ds-color-swatch`, `.ds-component-state`, `.ds-state-label`, `.glass-effect`, `.gradient-text`, `.glass-button`, `.icon-circle` |
| Future UI | `.font-oswald`, `.font-space`, `.rounded-full`, `.flex`, `.items-center`, `.relative`, `.border`, `.uppercase`, `.tracking-widest`, and keyframes `float1`, `float2`, `float3`, `spin1`, `spin2` |

The visual result is not a collage of three pages. Their reusable contracts were preserved while layout, palette relationships, component anatomy and choreography were redesigned as one system.
